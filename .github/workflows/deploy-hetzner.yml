name: Deploy to Hetzner

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        run: |
          docker buildx build \
            --platform linux/amd64 \
            --tag openclaw:latest \
            --tag openclaw:${{ github.sha }} \
            --load \
            .

      - name: Save Docker image
        run: |
          docker save openclaw:latest | gzip > openclaw-latest.tar.gz

      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.HETZNER_SSH_KEY }}
          known_hosts: unnecessary
          if_key_exists: replace

      - name: Add known hosts
        run: ssh-keyscan -H 5.161.46.0 >> ~/.ssh/known_hosts

      - name: Transfer Docker image to server
        run: |
          scp -o StrictHostKeyChecking=no openclaw-latest.tar.gz root@5.161.46.0:/tmp/

      - name: Deploy on server
        run: |
          ssh -o StrictHostKeyChecking=no root@5.161.46.0 << 'ENDSSH'
            # Load new image
            docker load < /tmp/openclaw-latest.tar.gz

            # Pull latest code
            cd /opt/openclaw
            git pull origin main

            # Restart services
            docker compose down
            docker compose up -d

            # Cleanup
            rm /tmp/openclaw-latest.tar.gz

            # Show logs
            docker compose logs openclaw-gateway --tail 30
          ENDSSH
